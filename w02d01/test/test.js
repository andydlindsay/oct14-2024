// describe(); // creates a heading for your tests (optional)

// it(); // actual test

// quality assurance (QA)

describe('tests for mocha', () => {

  it('runs the test', () => {
    // if the callback runs without error, test passes
    // if the callback encounters any error, test fails
    // throw new Error('ooooops');
  });

});
